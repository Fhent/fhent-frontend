import { Button } from "./ui/button";

export default function BridgeButton() {
  return (
    <Button className="rounded-[12px] border bg-transparent transition-colors duration-300 hover:bg-primary">
      Bridge
    </Button>
  );
}
